# This app currently has no required environment variables.
#
# Vite only exposes browser variables prefixed with VITE_.
# Example for a future public browser-safe value:
# VITE_PUBLIC_SITE_URL=https://example.com
#
# Do not put private server secrets in VITE_ variables.
