# Server-only deployment secret loaded by deployment/server.js from the repo root.
DEPLOY_TOKEN=CHANGE_ME_TO_A_LONG_RANDOM_SECRET
#
# Vite only exposes browser variables prefixed with VITE_.
# Do not put private server secrets in VITE_ variables.
