# Presentation Palette Perfect: Codebase Analysis and Principles

> **Core product intent:** This is a PowerPoint + portfolio website hybrid. Every slide is a presentation slide first — one dominant message, bold visual, minimal reading required. Think deck, not blog post.

## 0. Product Philosophy

### Visual-First, Always
- Each slide communicates **one idea**. If a slide has more than one primary message, it should be split or simplified.
- Text is support material. The visual — stat, image, animation, graphic — is the hero.
- If the slide can't be understood in 3 seconds of glancing, it needs redesigning.

### Message Hierarchy
- **H1-level:** The one thing you want them to remember. Big, bold, center/dominant.
- **H2-level:** One supporting detail or proof point. Smaller. Present only if needed.
- **Body-level:** Optional context. Never more than 2 sentences. Palanquin, muted color.
- **No paragraphs in slides.** Ever. Use fragments, stats, or single punchy lines.

### Lightweight as a Feature
- JS bundle target: under 800KB gzipped. Current: app chunk 165KB + vendors parallel-loaded ✓ (code-split via `vite.config.ts` manualChunks).
- Images: all deliverable assets must be WebP. PNGs allowed only as originals in `/src/assets` during dev. Run `npm run images:convert` (requires `npm i -D sharp`) to batch-convert to WebP.
- When replacing client creatives from external folders such as Downloads, convert them to WebP before wiring them into the site. Raychem RPG's three active creative assets are the converted WebP files in `src/assets/`, not the source JPEGs in Downloads.
- Heavy effects (3D, particle systems, blur) are **gated on desktop only** via `useIsMobile()`. Never on mobile, except for foreground hero objects that have a deliberate mobile fallback/performance cap. **Documented exceptions (user-requested):** the Clients slide `PrismaticBurst` shimmer runs on mobile too (caps DPR at 1.25, pauses RAF offscreen), and the cover `Globe` runs on mobile (cobe is a light single-canvas renderer).
- Keep only the current slide plus immediate neighbors mounted. Placeholder sections preserve scroll height for the rest. This keeps the next/previous slide ready during scroll-snap transitions without keeping the whole deck's WebGL canvases, RAF loops, timers, or pointer listeners alive.
- Ambient WebGL backgrounds should cap device pixel ratio at `1.25` unless a specific foreground detail requires more. On Retina displays this is a major heat/lag control.
- Any future foreground WebGL hero object may use a higher desktop DPR only when the object carries brand detail. Keep mobile capped lower and document the exception in `context.md`.
- Any custom animation loop (`requestAnimationFrame`, interval, Anime.js timer/timeline, WebGL renderer) must pause or unmount when offscreen. Visibility gating is a product requirement, not a polish task.
- Lazy-load all case study images via Vite's `?url` + lazy `<img loading="lazy" />`.
- Code-split vendors: Anime.js, ambient 3D libraries, and UI libs — each in its own chunk (see `vite.config.ts`). GSAP was removed in Session 30; the orphaned lanyard 3D/physics stack and its `vendor-lanyard` chunk were fully removed in Session 32 (deps + asset + Lanyard component deleted). The largest chunk is now `vendor-3d` (`three` + `postprocessing` for Hyperspeed, desktop-gated; ~686 KB).

### Copy Discipline

- No agency-speak template on case studies. The pattern is **`Who they are. What we did.`** — never `Boosted / Drove / Amplified / Elevated [thing] through strategic [tactic]`. The seven case-study subtitles were rewritten to this pattern in Session 11. **Descriptions must be plain (not corporate) and outcome-based, and you must verify the client's real business before writing** — Session 32 found Baxsaa wrongly tagged "beauty commerce" when The Baxsaa Co is a Mumbai premium custom-packaging studio. When unsure of a client's identity, web-search first.
- Banned words across the deck: **strategic, amplified, elevated, boosted, drove, impactful, vibrant, compelling, powerful, Success** (as a heading suffix). These signal nothing and pattern-match to every other agency deck.
- Never rude — confidence without spite. Lines that knock other agencies or imply the client was broken before us are not on-brand.
- No em dashes. Use commas or periods. Mobile readability + matches the deck's terse rhythm.
- Service-card descriptions stay plainspoken, one sentence, never two clauses joined by "and that".
- Pillar labels use **B2B-buyer language**, not agency-internal jargon (e.g. "Demand Gen" not "Reach & Activation").

### Slide Design Rules
- One full-bleed background or effect per slide. Never two competing backgrounds.
- Stats and numbers: weight 900, `clamp` sizing, teal color. Units always smaller weight. For horizontal proof strips, keep each stat cell on a consistent rhythm: tabular numbers, even cell widths, non-adjacent percentage metrics where possible, and readable label pills/backplates.
- No carousels or tabbed UI unless absolutely necessary — every extra interaction is cognitive load.
- Anime.js is the deck's primary entrance-motion layer. The deck uses a visible entrance recipe: heading drop/spring, accent-word blur-to-sharp slide, creative/slider settle, and stat/icon pulse where useful. Services, Clients, and all case-study slides own their local entry motion through `nativeMotion`; `DeckTransitionLayer` should give those slides only the watery wash/crest, not a second content settle. Do not add broad scan-line sweeps or circular glow-orbit blobs unless explicitly requested.
- The cover (TitleSlide) is hook-led, not wordmark-led. The hero headline is `We turn [rotating industry] businesses into brands buyers actually [rotating trust word]`, with two teal dynamic pills powered by the local Anime.js `WordRotate` component. Industry words currently include Solar, Industrial, Chemical, Pharma, Manufacturing, Mobility, and Real Estate; buyer-confidence words currently include trust, choose, believe, prefer, and buy from. The two pills carry a small desktop `md:-ml-6` negative margin so the pill text optically left-aligns with the plain heading lines (the rounded pill padding otherwise pushes the text in). The `OWLSURF DIGITAL` lockup is centered on mobile and left-aligned on desktop with a space before `DIGITAL`, and directly under it sits the brand tagline `where tech meets design` — "where tech meets" in Montserrat (`font-sans`), "design" in the Lora cursive italic teal treatment (`font-serif italic text-owl-teal`), shown desktop-only (`hidden md:block`). The right side carries an editorial signal graphic (circular orbit rings + nodes + scan glow) around the OwlSurf mark; the boxy grid lines, bordered inset squares, and centre cross-lines were removed (Session 35) — do not reintroduce them. The `Review case studies` CTA sits above the partner badge strip and uses a single continuous teal shimmer sweep (`.ts-cta-sheen`); it has no gradient text-shimmer and no inline arrow (the decorative `Arrow19` already points at it) — do not re-add `AnimatedShinyText` or the inline `→`. The partner badge strip is exactly three badges — LinkedIn Marketing Partner, Google Ads Agency Partner, Meta Agency Partner (HubSpot was dropped Session 35) — each trimmed to content with `sharp.trim()` and shown on identical fixed-height white chips so the row is uniform and vertically centred. Do not reintroduce the old centered-wordmark hero, the literal portfolio-proof info box, the `What We Do` / `Made For` info columns, the `Credentials for chemical and industrial markets` subline, the `Strategy, content, demand…` paragraph under the hook, or the top-right `Credentials / 01` label unless explicitly asked. The cover `Globe` runs on mobile too (anchored to the bottom edge so ~3/4 shows, `opacity-60` for legibility) — see the WebGL exception note.
- The cover can carry compact credibility badges. Use actual badge image assets, keep them as a compact horizontal strip secondary to the wordmark/logo, and avoid decorative framing or per-badge frames. Crop source images tightly when badges contain excess white padding so visual size is controlled by the badge content, not transparent/white margins.
- Case studies use one of two layouts and reuse the same heading recipe (see below), swapping deck teal for a per-case-study brand color:
  - **Split / proof-strip** (e.g. Mitsui): headline and tagline can anchor top-left while a horizontal proof strip sits top-right and the creative carousel occupies the bottom-centered stage. If using an animated slider, keep structural centering transforms on a stable outer wrapper and animate an inner element so Anime.js does not overwrite layout transforms.
  - **Polished vertical** (e.g. Baxsaa): centered eyebrow + heading + tagline top, creative grid or `CaseStudyCarousel` middle, brand-tinted stat pills row, optional eyebrow-style callout card (e.g. Baxsaa's SEO clean-up) at bottom. Pills use a translucent fill + 1px brand-color hairline border instead of `LiquidGlassCard` so the brand palette reads cleanly on light or dark gradients.
- The desktop case-study creative stage is a Blossom "cards" stack (Session 31), not the old 3D parallax slider. `CaseStudyCarousel.tsx` (renamed from `ParallaxCardSlider` in Session 31) wraps `BlossomCarousel` on both platforms — mobile cover-flow, desktop card-stack — and the old `requestAnimationFrame` tilt/3D code was removed. The desktop recipe lives in `.cs-cards*` in `src/index.css`: a `--card-width`-driven scroll-snap grid with sticky slides, `view-timeline` + `cs-cards-stack`/`cs-cards-rotate` keyframes (uses `sibling-index()` for z-order), and an `@supports not (animation-timeline)` static fallback for Safari/Firefox. Cards are square (`aspect-ratio 1/1`) with `object-fit: contain` and a 1px inner inset so the whole creative shows without clipping; the stage is vertically centered (`md:top-[58%] -translate-y-1/2`) and shifted left (`md:left-[50%]`) to align with the proof table and leave room on the right (planned for video embeds — `.cs-cards-card video` is pre-styled). To resize, change `--card-width`; do not reintroduce the 3D tilt.
- Case-study stat pills use one unified pill shape across desktop and mobile (Session 31): `rounded-full`, a `value │ label` row with a brand-tinted left divider, sized with `md:min-h-[3.4rem]` (not a fixed height) so long values/labels grow instead of clipping. The stat container is `md:w-[68%] lg:w-[60%]` with tightened padding so long labels like "INTERACTION RATE" don't clip. Do not switch the desktop pills back to a fixed-height box.
- Mobile case-study slides use a dedicated compact stack: heading + subtitle, a tall Blossom cover-flow carousel (`.cs-coverflow-item` aspect `1/2`, `.cs-coverflow-card img` `object-fit: cover` so the image fills the card), a translucent proof table, and exactly four translucent stat pills where metrics exist. Keep the `Role` row, omit the `Proof` row, and remove the least useful fifth metric instead of forcing five pills. On mobile the stack is top-aligned (`justify-start`), the `Case proof NN` eyebrow is hidden (`hidden md:block`), and the stat pills are compact (`h-[2.2rem]`). The global nav bar (logo + menu) is hidden on the case-study slides (indices 4–11, `onCaseStudy`) on mobile so the cases read as immersive; desktop nav is untouched. The Session 28 real-phone black-screen bug is FIXED (it was the active-slide mount-index math, not the carousel — see the mount-height note in section 2).
- Animations reveal content, never distract from it. Duration cap: 1200ms max per ordinary element, except a deliberate foreground hero reveal or deck-level liquid settle can run longer when it reads as one object. Avoid sharp end snaps; prefer Anime.js `cubicBezier`, `spring`, and shared slide-motion helpers over CSS keyframes for primary slide entry.
- Slide 2 Who We Are is a full-width editorial layout over the Hyperspeed running-lines background on desktop and a compact buyer-outcome layout on mobile. Structure is three clear blocks: top-left message (`What we understand`), top-right `Priority sectors` rail, and a full-width `What this means for buyers` outcome row/list across the bottom. The current headline is `Long sales. Technical buyers.`, with `sales.` and `buyers.` in the teal cursive serif treatment. Keep the running lines visible on desktop and keep mobile inside one viewport. On mobile the three blocks breathe more (centered, larger headline): `Priority sectors` render as an auto-scrolling CSS marquee of teal pills (two duplicated sets, `translateX(-50%)` loop, edge mask-fade, paused under reduced motion) instead of the desktop vertical list, and `What this means for buyers` is a two-column card grid with the final card spanning full width. The marquee keyframes live in `src/index.css` (`who-sector-marquee`). Do not use the `LogoLoop` component for the sectors — it measures sequence width from `<img>` children and over-duplicates with icon pills. Do not reintroduce the old split layout, the engineering illustration, per-row numbering, the wide rule/em-dash styling, `Why this replaces the PDF`, or the old generic `Deck to demand` phrasing unless asked.
- The cover slide's right-side logo treatment is now an editorial signal map, not a proof card: technical grid, quiet rules, orbit rings, signal nodes, subtle scan sweep, and the animated `OwlSurfLogo` SVG as the focal point. Keep it graphic and non-literal; avoid restoring checklist/card/table copy on the cover.
- Local UI-library-inspired blocks are acceptable when they avoid dependency churn. Adapt external component patterns into local React/Tailwind 3 components unless the user explicitly asks to install the library stack.
- Web-preview elements inside slides should be self-contained and stable. Prefer `srcDoc` or local preview markup over external iframes that may be blocked by frame policies.
- The Our Team slide was removed in Session 30 (`OurTeamSlide.tsx` deleted). The deck no longer has a team slide. The lanyard component (`src/components/ui/Lanyard/`), its `card.glb` asset, and its Three.js/Rapier/drei/fiber deps were fully deleted in Session 32. Do not re-add a team slide or reinstall the lanyard stack unless explicitly asked.
- CardSwap (Services slide stack, desktop) is built on Anime.js (Session 30 — GSAP was fully removed from the project). The swap sequence uses `createTimeline` and the loop uses `createTimer`, so it stays inside Anime's engine (which keeps a live rAF while animations/timers exist); do not revert to `setInterval`. Card self-centering is done with CSS negative margins (not GSAP `xPercent/yPercent`) so the transform stays free for Anime to animate translate/skew.
- Services slide ("Buyer Systems") is a left-pillars / right-CardSwap layout, NOT a flat icon grid. The five pillars use plain B2B buyer language: Technical Story, Demand Capture, Search & Trust, Sales Stack, AI Workflow. The slide subtitle is the short glanceable line `Five systems that make a complex product easy to buy.`. Do not restore the old dense copy about assets and operating layers around a technical sale. Each pillar holds 5 sub-services shown as stacked CardSwap cards on desktop. Card headings are teal; bodies are plainspoken, one short sentence, readable at a glance, and use no em-dashes. Keep necessary industry terms (ABM, SEO, CRM, HubSpot/Marketo) but avoid obscure acronyms: write `AI Search Optimization` (not `Answer Engine Optimization / AEO`) and `Marketing Assistants` (not `Marketing Copilots`). All service copy must read the same plainspoken way on desktop and mobile. Text content is shared, so simplify both together. The first `Technical Story` vertical has a two-column card body with text left and a clip-art SVG illustration right; keep the illustration contained and non-clipping. The other verticals stay text-only unless asked. The per-card `[PILLAR] • B2B / Industrial` footer band was removed in Session 11; do not reintroduce it. The active pillar tab is the cue. Mobile is a separate `isMobile` branch (desktop CardSwap is never touched): all five categories show as a two-row segmented icon bar (3 + centered 2, no hidden horizontal scroll), with a dynamic `INSIDE {category}` header above the panel. Do not restore the rogue `BUILD ORDER` text or the old "Tap a system..." hint. The active category renders as an Anime.js-driven build-sequence stepper on a teal spine, titles only (the `01/02...` numbering was removed in Session 32), consolidated to three headline groups per category via the `mobileServices` map in `ServicesSlide.tsx`; descriptions are intentionally dropped on mobile. Keep desktop showing the full five services with descriptions.
- Clients slide (slide 3) uses a top-left heading that reads only `CLIENTS` plus centered React Bits `LogoLoop` rows. Keep true CSS mask edge fading, calm speeds, pause-on-hover, and offscreen RAF pausing. Do not reintroduce the word Proof, a proof eyebrow, or the four market-note lines below the heading unless explicitly asked. A mobile-only (`md:hidden`) credibility card strip sits below the logo loops to fill the empty mobile space (`End-to-end`, `6 Sectors`, `India + APAC`, `Meta + Google`, `B2B`); the PrismaticBurst backdrop now runs on both desktop and mobile (user-requested mobile exception; see the heavy-effects exception note). Do not put a client count on this strip. The deck should not advertise how few logos there are.
- Contact slide is a centered closer (redesigned Session 29, tuned Session 31): an OwlSurf ripple mark hero (`md:w-[300px]`, shown on mobile too), a `Let's talk` eyebrow, a `We make the [rotating] easy to [rotating]` headline where both accent words are teal block-uppercase pills driven by the local `WordRotate` component, a primary email CTA button plus a ghost phone CTA button, and the compact footer block. Session 31: the headline is one continuous line on desktop (`flex md:flex-nowrap md:max-w-none`, static words `shrink-0 whitespace-nowrap`) and wraps only on mobile (`flex-wrap`); font is `md:text-[clamp(1.7rem,3.1vw,2.6rem)]` (pills `clamp(1.5rem,2.7vw,2.4rem)`) so the widest combo fits without clipping off-screen. The pills keep dynamic per-word width (the `WordRotate` shell animates to `measure.offsetWidth + 6` — the +6 buffer stops the last glyph clipping under `overflow-hidden`; do not re-lock the width). Word sets are the "Clarity" theme: `rotatingComplexity = [complex, technical, complicated, confusing, overwhelming]`, `rotatingChoose = [understand, explain, buy, get, choose]` — keep them everyday words since the two pills rotate independently and every combo must read naturally. The email + phone CTAs are `shrink-0 whitespace-nowrap` so the number never wraps into a deformed circle. On mobile the column is top-aligned (`justify-start`) and desktop is centered. Do not reintroduce the old `LET'S MAKE / COMPLEX / obvious.` headline, the `If this replaces the portfolio PDF…` line, the four `white/12` frame border lines, the desktop-only side aside, headers, credentials labels, or explanatory close text unless explicitly requested. The footer (`FlyonFooter`) carries only the OwlSurf logo, three social badges, and the copyright — phone/email/owlsurf.com live as the slide's CTA buttons, not in the footer. Session 38: the `Bring the product, the market, and the sales problem…` paragraph was removed entirely (both platforms); on mobile the column is now vertically centered (`justify-center`, `pb-[6.75rem]`), the ripple mark is `248px`, and the two CTAs are a full-width equal pair (`flex-1`, `py-3.5`, labels `Hi` / `Call us`) — desktop classes restored via `sm:`/`md:` tokens.
- The site has a route-level branded 404 page outside the slide matrix. Unknown paths should go there through React Router's catch-all route, not become another presentation slide.
- Unified heading recipe across slides 2–5: eyebrow (10px tracking-[0.3em] teal) + `clamp(3.4rem,5.9vw,6.6rem)` Montserrat black h2, white first word + teal-gradient second word, left-aligned via a `w-full h-full` wrapper that defeats `.slide`'s `items-center justify-center`. Always set `font-sans not-italic` on bare spans inside the h2 to avoid the `span:not([class])` Palanquin/italic footgun.
- Case studies share the same recipe family but with four differences: (1) the h2 clamp drops to `clamp(2.6rem,4.6vw,5.2rem)`-ish so the brand name fits a half-column or stays clean, (2) the eyebrow + accent color is the case study's own brand color (Mitsui cyan, Baxsaa maroon, etc.) instead of OwlSurf teal, (3) the first word/text color flips per background: white ink on dark gradient backgrounds, dark ink (`hsl(0 0% 15%)`) on light gradient backgrounds, (4) the proof/info panel sits in the same vertical band as the creative carousel on desktop. Subtitle/tagline text should stay Palanquin, larger than body copy, and readable at a glance.

### Accessibility & Motion (added Session 23)

- **Real semantics, not decoration roles.** Navigation is a plain `nav > ul > li > button` — never `role="menubar"`/`menuitem"` (that implies app-menu keyboard behavior the deck does not provide). Active nav items use `aria-current="page"`. Disclosure controls (the mobile hamburger) expose `aria-expanded` + `aria-controls`, and the disclosed menu closes on Escape, takes focus on open, and returns focus to its trigger on close.
- **The Services pillars are a tablist.** They use the WAI-ARIA tabs pattern: `role="tablist"`/`tab"`/`tabpanel"`, `aria-selected`, `aria-controls`/`aria-labelledby`, roving `tabIndex`, and Arrow/Home/End keyboard nav. Keep this pattern if the tabs are restyled.
- **Contrast is a requirement.** Body and roster text must meet WCAG AA. The Our Team roster keeps the active name in teal and inactive names readable (`text-white/65`+), not the old `text-white/22`. Do not drop inactive content text below a legible contrast just for a depth effect.
- **Heavy WebGL is desktop-only by default and enforced in code.** `LightRays` is gated behind `useIsMobile()` and must not mount below 768px. **Explicit exceptions (user-requested):** `PrismaticBurst` runs on mobile on the Clients slide (caps DPR 1.25, pauses RAF offscreen), and the cover `Globe` (cobe, a light single-canvas renderer) runs on mobile on the TitleSlide. Keep all other heavy effects gated. `Hyperspeed` was temporarily re-enabled on mobile during the live Slide 2 animation pass, but it is still unreliable on the user's phone and should be revisited after the case-study black-screen issue. Gate any new heavy effect the same way unless the user explicitly asks for a mobile animation exception.
- **Respect `prefers-reduced-motion`.** Use `usePrefersReducedMotion()` (`src/hooks/use-reduced-motion.tsx`). It already gates the deck `scrollTo` (smooth → instant), the roster auto-advance (pauses), and `CardSwap` (the `reduceMotion` prop snaps cards instantly but keeps cycling so back-card content stays reachable). When adding new auto-advancing or large entrance motion, gate it through this hook. Auto-advancing content should pause or become instant under reduced motion, and must not become unreachable.
- **No black flash between slides.** `SlideFallback` is a soft branded skeleton, not an empty section. `SLIDE_MOUNT_RADIUS` stays at `1` so the next and previous slides are ready during natural scroll-snap movement; keep the radius tight and rely on the skeleton for farther-off slides.
- **Social/OG image** must be an OwlSurf-branded asset, never a generic placeholder. `index.html` currently uses local `/favicon.png` as a stopgap; a true 1200×630 card is preferred.

### Mobile Layout (added Session 24)

- **Every slide must fit one mobile viewport.** No content clipped at the top or bottom edge on phones. Mobile changes are gated with default/`max`-width Tailwind classes plus `md:` tokens that restore the desktop values verbatim, or with `useIsMobile()` branches. Desktop must stay byte-for-byte identical when editing mobile — verify with a desktop screenshot, not just a phone one.
- **Heavy/oversized widgets get compact mobile variants, they are not just shrunk.** The Services 3D `CardSwap` stack overflows phones, so mobile uses a two-row segmented category bar plus a numbered stepper (titles only, three consolidated groups per category) instead — see the Services slide rule above. Do not try to make `CardSwap` "fit" on mobile by scaling — use the mobile variant.
- **Long content rows become horizontal swipe rows on mobile**, not tall vertical stacks (e.g. the Who We Are differentiator cards: `flex overflow-x-auto` on mobile, `sm:grid sm:grid-cols-3` from 640px up). This keeps the slide inside one viewport without an internal vertical scroll that would fight the deck's scroll-snap.
- **Keep slides inside the scroll-snap height.** Do not make a single mobile slide taller than the viewport to "solve" overflow; that breaks `scroll-snap-type: y mandatory`. Compact spacing/type and swap widgets instead.
- **Mobile verification harness:** `scripts/mobile-shots.mjs` (Playwright, kept as a devDependency) screenshots every slide at a phone viewport against the running dev server. `SHOT_TAG`, `SHOT_W`/`SHOT_H`, `SHOT_ONLY` (comma slide indices), and `SHOT_DESKTOP=1` env vars control it. Output under `scripts/_shots/` is gitignored. Use it before declaring mobile work done.

### Mobile Motion & Transitions (Session 36)

The **mobile** deck motion was rebuilt from scratch and is gated entirely behind `useIsMobile()` / `@media (max-width:767px)`. **Desktop must stay byte-for-byte identical** — it keeps mandatory scroll-snap, the animejs `SlideReveal` reveal, and the animejs `DeckTransitionLayer` liquid-wash, and never syncs the URL. Do not let mobile motion edits leak into the desktop branches.

- **Way A — scroll-synced URLs.** The deck is still one continuous scroll page, but each slide has a real URL slug (`src/pages/slide-routes.ts`; routes registered in `App.tsx`, all rendering the same `<Index/>`). On mobile, scrolling syncs the URL to the slide in view (debounced), so slides are deep-linkable and back/forward steps between them. Desktop never syncs (a deep link only scrolls once on mount). The two effects in `Index.tsx` guard against a navigate↔scroll loop via `suppressUrlSyncRef` + `currentSlideRef` — keep both when editing.
- **Light snap, not rigid.** Mobile uses `scroll-snap-type: y proximity` (friction + settle into place) instead of desktop's `y mandatory`. Do not restore mandatory snap on mobile.
- **Slide cross-fade** is Motion (`motion.dev`), not animejs: `MobileSlideMotion.tsx` wraps each slide via `SlideReveal`'s mobile branch and drives opacity/translateY/blur from `useScroll` against the deck container (passed through `DeckScrollContext`). **Never add a `scale` transform to that wrapper** — it is an ancestor of `.slide` and scaling corrupts the `getBoundingClientRect` height `getSlideHeight` relies on for the scroll→index math.
- **Mobile entrances never spring (Session 38).** All animejs spring eases are desktop-only — on phones the overshoot lands on top of the proximity-snap settle and reads as jank. `getSlideContentEase(isMobile)` in `slide-motion.ts` returns `slideSettleEase` on mobile and the springs on desktop; `animateSlideHeading` also switches to the settle ease + smaller scale (0.985) on mobile. Mobile content entrances use smaller rises (14-18px) and no/minimal scale. Do not add spring eases to mobile entrance paths.
- **Mobile copy may be shorter than desktop (Session 38).** The old strict "copy is shared desktop↔mobile" parity is relaxed: mobile may carry a tighter variant of the same message (never a different message). Mechanisms: `mobileSubtitle` prop on `CaseStudyLayout` (all 7 layout case studies have one), the inline `isMobile` subtitle branch in `CaseStudySlide` (Mitsui), `mobileOutcomes` (Who We Are), and `mobileServices` titles (Services). Desktop strings stay the fuller lines.
- **Staggered slide entrance (Session 37).** On top of the cross-fade, non-native-motion slides (Cover, Who We Are, Contact) get an animejs staggered child reveal in `MobileSlideMotion`: when the slide settles ≥50% in view, top-level elements rise in one by one (110ms stagger, 560ms each), replaying on each revisit (re-armed when fully offscreen). Native-motion slides (Services, Clients, case studies) skip it — they own their own animejs entrances, no double motion. Reduced motion: static.
- **Seam blend is color-aware (Session 37).** The old fixed dark-only `.deck-seam-fade` viewport overlays are gone (they tinted the light case studies dark at the edges, which itself read as a divider). Each slide joint now blends through the 50/50 `color-mix(in oklab, …)` of the two adjacent slides' edge colors: short 6.5svh gradients (with a 45% mid-stop so the color releases quickly) on both sides of the seam meet at the same mixed color (rendered in `SlideReveal`'s mobile branch). Saturated edge colors are deliberately desaturated/darkened where the mix would otherwise band (VNT bottom, Kuraray bottom, GirlUp bottom — Session 38). Per-slide edge colors live in `src/pages/slide-edge-colors.ts` — **deck-order or slide-background changes must update that file** (same bookkeeping as `Index.tsx`/`PillNav`).
- **Signature transition is the "liquid crest" (Session 37, shipped).** `MobileTransitionLayer.tsx` replaces `DeckTransitionLayer` on mobile: a thin teal hairline + soft bloom **position-locked to the physical seam between two slides**, travelling with the finger through the viewport — the mobile analog of the desktop liquid-wash. Opacity uses a dt-based eased follower (fast attack, ~450ms release) so the proximity snap can't reduce it to a blip and the idle stop fades instead of snapping; **position is never eased** so the crest stays glued to the seam. The old full-bleed teal wash and the centred circular glow mark (the "stray circle") are gone. Look params (crest opacity, bloom blur, crest scale) are a Theatre.js object in `src/theatre/deck.ts`; studio auto-init stays disabled (re-enable the guarded dev-only `import("@theatre/studio")` to author). `@theatre/studio` is a devDependency and must never ship to prod.
- animejs stays the in-slide entrance engine on both platforms; only the deck-level transition + reveal wrapper changed on mobile.

This document serves as a comprehensive overview of the design schemas, structural paradigms, and coding principles adopted in this project. It is intended to guide future development, maintenance, and refactoring efforts.

## 1. Architectural Overview
The codebase is structured as a **Single-Page Application (SPA)** using **Vite + React (TypeScript)**. 
Unlike traditional multipage websites, it adopts a **Vertical Scroll-Snapping Presentation** architecture.
- **Entry Point:** The application essentially routes entirely through `App.tsx` into `Index.tsx`.
- **Slide Array Pattern:** `Index.tsx` mounts an array of `SlideComponent`s. These components represent sequential, full-screen vertical sections.
- **No Our Team slide.** It was removed in Session 30. Deck order (13 slides since Session 38): Cover (0), Who We Are / Positioning (1), Services / Buyer Systems (2), Clients (3), eight case studies (4-11), Contact (12). Session 35 added **Kuraray** after Mitsui and removed **Raychem RPG** entirely; Session 38 added **DEHN** (German lightning/surge protection and earthing maker, brand red `353 100% 44%`, six WebP creatives converted from Downloads `DEHN SLIDE IMAGES`) as case `08` after VNT. Current case order: Mitsui (01), Kuraray (02), Baxsaa (03), CultFit (04), GirlUp (05), CTP (06), VNT (07), DEHN (08). Any change to the case-study count must also update `Index.tsx` (slides array + `onCaseStudy` range + `nativeMotion` range), `PillNav` (Contact `slideIndex` + `slideToNavIndex`), `DebugMenu` labels, `slide-routes.ts` (slug list), and `slide-edge-colors.ts` (seam colors).
- **Who We Are Slide:** `SkyrocketSlide.tsx` is now the main slide-2 editorial statement. It uses a full-width editorial layout over the Hyperspeed running-lines background: message block, industries rail, and differentiator row. The old right-aligned technical illustration asset was removed.
- **Removed About Animation:** The old separate third `WhoAreWeSlide` and `Ballpit` animation were removed to keep the deck lighter and avoid redundant "Who We Are" messaging.
- **Scroll Hijacking:** Native CSS scroll behaviors (`scroll-snap-type: y mandatory`, `scroll-snap-align: start`) are used heavily on `.slide` elements to emulate a traditional "PowerPoint-like" or "Deck-like" feel on the web.
- **Header Navigation:** `PillNav` is global but activity-driven. It appears while the user is moving through the deck and hides after a short idle delay so each slide can be viewed cleanly.
- **Case Study Sub-Stack:** Slides 4 through 10 (indices) form a continuous block of seven interactive case studies: Mitsui, Kuraray, Baxsaa, CultFit, GirlUp, CTP, VNT (Session 35: Kuraray added after Mitsui, Raychem RPG removed).

## 2. Code Principles

### Minimal State Management
- **Local React State:** There is deliberately no heavy global state management library (like Redux or Zustand). 
- **Prop Drilling for Core Interactions:** Features that require cross-component communication, like letting a specific slide trigger navigation (`onViewCaseStudies`), pass navigation handlers via props down to the slide component.
- **Navigation via Refs & Scrolling:** Slide transitions are managed natively by tracking the container scroll offset (`scrollTop / slideHeight`) inside `Index.tsx`, and explicit navigation triggers a `scrollTo` invocation on the `containerRef`. 

### Performance and Separation of Logic
- **Decoupled Animations:** Complex enter/exit animations are decoupled from standard rendering logic. Component `SlideReveal.tsx` acts as an Intersection Observer wrapper and can mark slides as `nativeMotion` when a slide owns its own Anime.js sequence. Native-motion slides skip the wrapper reveal so content is not animated twice.
- **Mobile vs Desktop Branching:** `CaseStudyCarousel.tsx` (renamed from `ParallaxCardSlider`) branches on `useIsMobile()` into two `BlossomCarousel` recipes — mobile cover-flow and desktop card-stack (Session 31). Both are scroll/`view-timeline`-driven CSS, not JS animation loops; the old desktop `requestAnimationFrame` tilt was removed. Other computationally intensive effects (WebGL backdrops) remain gated behind `useIsMobile()` per the Lightweight rules above.
- **Active Slide Lifecycle:** `Index.tsx` should keep the current slide plus immediate neighbors mounted. This is the transition-smoothing window; anything farther away remains a placeholder so cumulative WebGL/RAF load stays bounded.
- **Slide-index math must use a measured slide height, never `window.innerHeight`.** `Index.tsx` derives the active slide from `scrollTop`, and the programmatic `scrollTo` multiplies by the per-slide stride. Use `getSlideHeight(container)` (measures a real `.slide` via `getBoundingClientRect`), not `window.innerHeight`: on mobile Chrome the URL bar auto-hides and `window.innerHeight` grows while the slides stay a constant height, so the index drifts low and compounds down the deck. Combined with a tight mount window, that drift can mount the wrong slide and show `SlideFallback` (the Session 28 real-phone black-screen bug). Do not reintroduce `window.innerHeight` into the slide-index or `scrollTo` math.
- **Animation Ownership:** If a component owns a RAF loop, WebGL renderer, interval, or pointer listener, it must clean it up on unmount and ideally pause it with IntersectionObserver when merely offscreen.
- **Contact Logo Centering:** The contact-slide logo/ripple group uses a positioning wrapper plus a separate animated inner wrapper. Do not animate transforms on the positioned wrapper, or the logo will lose center alignment.

## 3. Design and Aesthetic Principles

### Tailwind + Fixed Dark Theme
- **CSS Variable Injection:** Global styling overrides are defined fundamentally using CSS custom properties inside `@layer base` in `src/index.css`. This enforces a strict color token system (e.g. `--primary`, `--card`, `--owl-green`).
- **Fixed OwlSurf Dark Mode:** The deck no longer exposes a light/dark toggle. The dark OwlSurf presentation theme is the single production visual system.
- **Tailwind Utility Classes:** Components rely extensively on Tailwind classes for scaffolding, structure, layout (`flex`, `grid`, `absolute inset-0`), and standard spacing. 

### Fluidity and Animations
- **Anime.js for All Sequencing:** The project was migrated entirely away from Framer Motion. `Anime.js` now exclusively handles staggered entrance animations (resolving opacity, translation, scale) directly in components like `TitleSlide.tsx`, through shared slide-motion helpers, and through the deck-level transition layer.
- **Anime.js is the only animation library.** GSAP was removed in Session 30. `PillNav` (show/hide, hover circle-fill, mobile menu) and `CardSwap` (Services stack) are built on Anime.js (`animate`, `createTimeline`, `createTimer`, `utils`). Do not reintroduce GSAP.
- **CSS Transitions:** Standard hover effects and states explicitly specify easing parameters, ensuring fluid hover state inversions. Easing curves like `cubic-bezier(0.4, 0, 0.2, 1)` are used over harsh linear jumps.

### Aesthetic Patterns "The Glass and Glow"
- **Glassmorphism:** Navigation menus (`PillNav`), popovers, and specific slide cards rely on `backdrop-filter: blur() saturate()`. Elements are intended to sit fluidly over complex background arrays or 3D visuals.
- **Soft Emittance (Glow):** Gradients (`.text-gradient-green`, `.glow-green`) and box-shadow pulses give elements a neon, high-contrast, cyberpunk-adjacent glow against the otherwise dark backgrounds. Ensure any primary accent is accompanied by an underglow to give an illusion of light emissions.

## 4. Deployment Principles

> The live site at https://www.owlsurf.media is push-to-deploy. Full mechanics and the recovery runbook live in `context.md` under "Deployment Pipeline & Operations" — this section is the short operational contract.

- **A push to `main` is a deploy.** `.github/workflows/deploy.yml` POSTs `https://www.owlsurf.media/deploy` (token-gated), the VPS webhook (`deployment/server.js`) spawns `deployment/deploy.sh` detached, and that script does `git reset --hard origin/main` → `npm ci` → `npm run build` → `pm2 restart heyowlsurf`. Live in ~10-60s. Treat every push to `main` as a production release, not a checkpoint.
- **The Actions `200` is an ack, not a success.** The build runs detached after the response returns. A green Actions run means the webhook was received, not that the build passed. Confirm the live site visually after a push.
- **Deploy failures are silent.** No alerting. If a push doesn't appear live, check `gh run list`. A `curl: (28) ... Timeout` on port 443 is transient VPS unavailability (often build memory pressure from the `vendor-3d` Three.js chunk), not a repo bug. Retry flags for the webhook exist locally but are not pushed yet because workflow-file changes require `workflow` auth scope; for an already-failed run, recover with `gh run rerun <id>`. See the 2026-06-04 incident in `context.md`.
- **Never hand-edit files directly on the VPS.** `deploy.sh` runs `git reset --hard origin/main` on every deploy, which discards any uncommitted server-side edits. All changes go through `main`.
- **Do not commit secrets to the deploy path.** `DEPLOY_TOKEN` lives in GitHub Actions secrets and the VPS `.env` only. Standing security debt: the GitHub PAT currently in plaintext in `.git/config` must be rotated and the remote moved to SSH or a credential helper.

## 5. Next-session backlog — Hallmark audit (Session 29, no edits applied)

A `hallmark audit` was run against the deck (editorial genre, `design.md`-managed). The structure passed — no AI-template fingerprint, 0 critical findings. The full punch list with file:line refs lives in `context.md` under "Next-session plan — Hallmark audit punch list". Priority order for the next pass:

1. **`transition-all` → named-property transitions** (`TitleSlide.tsx:362`, `SkyrocketSlide.tsx:264`, `ServicesSlide.tsx:465,551`). Never animate `all`. (The old `ParallaxCardSlider.tsx` instance is gone — that component was rewritten to the Blossom card stack and renamed `CaseStudyCarousel.tsx` in Session 31.)
2. **Add `:focus-visible` rings** (≥3:1 contrast, instant, never animated) to all custom interactive elements — Contact CTAs, cover CTA, Slide-2 chips, Services tabs, nav buttons. Only `LogoLoop.css` has one today. Real WCAG gap.
3. **Trim eyebrows** — 11 uppercase tracked kickers across slides; cap at 1–2, keep only genuinely-ordinal ones. Half-sanctioned by the "credentials rhythm" rule, so it's a brand-vs-tell call.
4. **Gradient headline decision** — the `text-gradient-green` accent word is the canonical editorial AI tell but is a mandated brand signature here; decide keep vs solid-teal-with-weight.
5. Minors: `tabular-nums` on all numeric stats; curly apostrophes in copy; keep scroll-reveal/`backdrop-blur` breadth in check; replace `logo-main.jpg` (JPG → WebP/SVG); ship a true 1200×630 OG card.

0 critical · 4 major · 6 minor. Verdict: close, fix the majors — focus rings and `transition-all` are the highest-value, lowest-risk wins.
